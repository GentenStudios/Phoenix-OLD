set(currentDir ${CMAKE_CURRENT_LIST_DIR})
set(Sources
        ${currentDir}/Server.cpp
        ${currentDir}/Iris.cpp
        ${currentDir}/Game.cpp
        ${currentDir}/Commander.cpp

        ${currentDir}/Main.cpp

        PARENT_SCOPE
        )
